defaults: &defaults
  host: 127.0.0.1
  port: 27017

development:
  <<: *defaults
  database: factorypeople_development

test:
  <<: *defaults
  database: factorypeople_test

# set these environment variables on your prod server
production:
  <<: *defaults
  database: factorypeople
  username: <%= ENV['MONGO_USERNAME'] %>
  password: <%= ENV['MONGO_PASSWORD'] %>

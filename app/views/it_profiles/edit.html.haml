%h2= "Editing IT profile for #{@person.full_name}"
= render 'form'
- if can?(:manage, @person)
  = [ link_to('Show', @person), link_to('Back', person_it_profile_path(@person, @it_profile)) ].join(' | ').html_safe

%h2= "#{pronoun_or_first_name(@person)} Credentials"
= render 'form'
- if can?(:manage, @person)
  = [ link_to('Show', @person), link_to('Back', person_emergency_profile_path(@person, @crednetial)) ].join(' | ').html_safe

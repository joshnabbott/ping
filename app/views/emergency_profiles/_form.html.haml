- semantic_form_for [@person, @emergency_profile], :url => person_emergency_profile_path(@person) do |form|
  = form.inputs :name => "Emergency Profile" do
    = form.input :name
    = form.input :phone_number
    = form.input :relation
  
  = form.buttons do
    = form.commit_button "Update"